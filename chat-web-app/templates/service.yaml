apiVersion: v1
kind: Service
metadata:
  name: {{ include "chat-web-app.fullname" . }}
  labels:
    {{- include "chat-web-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chat-web-app.selectorLabels" . | nindent 4 }}
